@model Blogger.Models.Post


<div>

    <h2>@ViewBag.Title</h2>


    @if (User.Identity.IsAuthenticated && User.Identity.Name == Model.PosterEmail)
    {
        @Html.ActionLink("", "GetEditPostView", "Post", new { id = Model.PostId }, new { @class = "btn btn-primary primaryglyphicon glyphicon-wrench pull-right" })
    }

</div>
 <br />
        <center>
            <h3><strong>@Model.Title</strong></h3><br>
           
       </center>
        
        <h4>@Model.Text</h4><br>
        Posted by @Model.PosterEmail at @Model.TimeStamp
   

